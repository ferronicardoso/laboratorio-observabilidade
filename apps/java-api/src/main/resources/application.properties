# Server
server.port=8080
spring.application.name=java-api

# Actuator (expor endpoints)
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true

# Métricas - Histogramas e Percentis
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.slo.http.server.requests=50ms,100ms,200ms,500ms,1s

# Logging
logging.level.root=INFO
logging.level.com.observability.api=INFO
logging.level.org.springframework.web=WARN
logging.level.org.apache.tomcat=WARN

# HTTP Request Logging (desabilitado - usando LoggingFilter estruturado)
server.tomcat.accesslog.enabled=false

# Database (MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
